# Bouncing Portraits Screensaver

An interactive screensaver featuring floating portrait heads, personal details, and video interviews.

## Run Locally

**Prerequisites:** Node.js


1. Install dependencies: `npm install`
2. Run the app: `npm run dev`

The development server runs at `http://localhost:3000`.
